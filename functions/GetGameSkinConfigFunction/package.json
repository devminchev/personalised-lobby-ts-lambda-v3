{
  "name": "get-game-skin-config",
  "version": "0.2.12",
  "description": "Returns configuration for a game requested by game skin",
  "main": "app.js",
  "repository": "https://gitlab.ballys.tech/excite/native/applications/personalised-lobby-ts-lambda-v3",
  "relComponent": "igaming_lobby_game_skin_config_v3",
  "scripts": {
    "unit": "jest",
    "test": "jest --config jest.config.ts",
    "clean": "rm -rf node_modules coverage",
    "clean:lock": "rimraf package-lock.json yarn.lock"
  },
  "devDependencies": {}
}
