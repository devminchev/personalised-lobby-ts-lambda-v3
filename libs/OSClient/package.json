{
  "name": "os-client",
  "version": "0.61.0",
  "description": "Common utilities for personalized lobby",
  "main": "./index.ts",
  "types": "./index.ts",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf node_modules coverage; jest --clearCache",
    "clean:lock": "rimraf package-lock.json yarn.lock",
    "format:check": "prettier --check '**/*.{ts,tsx,js,jsx,json,md}'"
  },
  "dependencies": {
    "dynamoClient": "*"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0"
  }
}
